import React, { Component } from 'react';
import  PageLayout  from './FirstPage/PageLayout';

export class Home extends Component {
  static displayName = Home.name;
  render() {
    return(
      <PageLayout/>
    );
  }
}
